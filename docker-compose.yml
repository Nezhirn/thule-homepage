services:
  homepage:
    image: thuleseeker/thule:latest
    container_name: thule-homepage
    ports:
      - "8000:8000"
    volumes:
      # Persistent data (database + uploaded images)
      - ./homepage-data:/app/data
    environment:
      - TZ=Europe/Moscow
    restart: unless-stopped
